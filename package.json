{
  "name": "tokensmithy",
  "version": "1.0.0",
  "description": "A smart contract-based token minting and management platform that allows decentralized token creation and transfers.",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network sepolia"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.22.1"
  }
}
